<mvc:View
	controllerName="testing.controller.Worklist"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:semantic="sap.f.semantic"
	xmlns:l= "sap.ui.layout">

	<semantic:SemanticPage
		id="page"
		headerPinnable="false"
		toggleHeaderOnTitleClick="false">

		<semantic:titleHeading>
			<Title text="{i18n>worklistTitle}" />
		</semantic:titleHeading>

		<semantic:content>
			<Table
				id="table"
				width="auto"
				items="{
					path: '/BusinessPartnerSet',
					sorter: {
						path: 'CompanyName',
						descending: false
					}
				}"
				noDataText="{worklistView>/tableNoDataText}"
				busyIndicatorDelay="{worklistView>/tableBusyDelay}"
				growing="true"
				growingScrollToLoad="true"
				updateFinished="onUpdateFinished">

				<headerToolbar>
					<Toolbar>
						<Title id="tableHeader" text="{worklistView>/worklistTableTitle}"/>
						<ToolbarSpacer />
						<SearchField
							id="searchField"
							tooltip="{i18n>worklistSearchTooltip}"
							search="onSearch"
							width="auto">
						</SearchField>
						<Button icon="sap-icon://add" tooltip="Add a company" press="onAddButton"></Button>
					</Toolbar>
				</headerToolbar>

				<columns>
					<Column id="nameColumn">
						<Text text="{i18n>tableNameColumnTitle}" id="nameColumnTitle"/>
					</Column>
					<Column id="unitNumberColumn" hAlign="End">
						<Text text="{i18n>tableUnitNumberColumnTitle}" id="unitNumberColumnTitle"/>
					</Column>
				</columns>

				<items>
					<ColumnListItem
						type="Navigation"
						press="onPress">
						<cells>
							<ObjectIdentifier
								title="{CompanyName}"
								titleActive="true"
								titlePress="onShowDetailPopOver"/>
							<ObjectNumber
								unit="{Address/AddressType}"/>
						</cells>
					</ColumnListItem>
				</items>
			</Table>
		</semantic:content>

		<semantic:sendEmailAction>
			<semantic:SendEmailAction id="shareEmail" press="onShareEmailPress"/>
		</semantic:sendEmailAction>

	<semantic:dependents>
		<ResponsivePopover
			id="contactPopOver"
			title="Company contact info"
			class="sapUiPopupWithPadding">
			<content>
				<l:Grid>
					<l:content>
						<Text text="Address: {path: 'Address'}"/>
						<Text text="Phone Number: {path: 'PhoneNumber'}"/>
					</l:content>
				</l:Grid>
			</content>
		</ResponsivePopover>
	</semantic:dependents>
	</semantic:SemanticPage>

</mvc:View>